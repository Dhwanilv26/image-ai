export function isTextType(type:string | undefined){

    return type==='text' || type==='i-text' || type==='textbook';
}

// different value and use cases to implement when the selected items are text items

